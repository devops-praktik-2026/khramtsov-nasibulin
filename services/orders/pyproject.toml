[project]
name = "orders"
version = "0.1.0"
description = "Сервис заказов — заготовка, которую дописывают студенты"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    # Когда дойдёте до хранения заказов в базе данных, добавьте сюда
    # sqlalchemy, alembic и psycopg — так же, как это сделано в сервисе клиентов.
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-cov>=6.0",
    "ruff>=0.8",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["orders"]

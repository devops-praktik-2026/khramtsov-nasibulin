[pytest]
testpaths = services/accounts/tests services/orders/tests
addopts =
    -q
    --cov=accounts
    --cov=orders
    --cov-report=term
    --cov-fail-under=80
python_files = test_*.py
python_functions = test_*
